#...
workspaces:
  - name basic-auth
params:
    - name: repo_url
    - name: revision
#...
tasks:
  workspaces:
    - name: basic-auth
      workspace: basic-auth
#  ...
  tasks:
  - name: git-clone-from-catalog
      taskRef:
        name: git-clone #<1>
      params:
        - name: url
          value: $(params.repo_url)
        - name: revision
          value: $(params.revision)
#...
